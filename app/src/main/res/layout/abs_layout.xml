<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/left_header_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/left_header_image"
        app:srcCompat="@drawable/padre" />

    <TextView
        android:id="@+id/mytext"
        style="@style/TitleTextStyle"
        android:text="@string/app_name" />

    <ImageView
        android:id="@+id/right_header_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/right_header_image"
        app:srcCompat="@drawable/padres" />
</LinearLayout>